spring:
  datasource:
    url: jdbc:h2:mem:db_test;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

custom:
  jwt:
    secretKey: test-secret
  frontend:
    baseUrl: "http://localhost:3000"
    registerPath: "/register/ox-test"
    mainPath: "/main"

gemini:
  api:
    key: dummy-key
    url: http://dummy-url
    url-stream: http://dummy-stream

cloud:
  aws:
    s3:
      bucket: dummy-bucket
    region:
      static: ap-northeast-2
    credentials:
      access-key: dummy
      secret-key: dummy
